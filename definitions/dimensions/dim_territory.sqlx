config {
    type: "table",
    description: "Dimension table for sales territories, using the natural key.",
    tags: ["dimensions"]
}

-- This model creates a simple dimension table for sales territories.
-- It uses the natural business key (TerritoryID) as the dimension key.

SELECT
    territoryid as territory_key,
    territoryid as territory_id,
    name as territory_name,
    countryregioncode as country_region_code,
    `group`
FROM
    `team_day_2025_adventure_works_oltp.Sales_SalesTerritory`
